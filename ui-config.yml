---
type: showroom

# Set the left column width to 30%
default_width: 40
# Persist the URL state so browser refresh doesn't reset the UI
persist_url_state: true

view_switcher:
  enabled: true
  default_mode: split

tabs:
  - name: OpenShift Console
    url: 'https://console-openshift-console.${DOMAIN}'

  - name: Argo CD
    url: 'https://openshift-gitops-server-openshift-gitops.${DOMAIN}'

  - name: SonarQube
    url: 'https://sonarqube-sonarqube.${DOMAIN}/dashboard?id=parasol-insurance-user-${GUID}'

  - name: GitLab
    url: 'https://gitlab-gitlab.${DOMAIN}/'
